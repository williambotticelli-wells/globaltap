# This file is autogenerated with reference to psynet/resources/experiment_scripts/pytest.ini

[pytest]
log_cli = 1
log_cli_level = INFO

# This warning occurs when we patch Experiment attributes (e.g. test_n_bots) prior to invoking pytest
# from within Python. It should be safe to ignore, we think.
filterwarnings =
    ignore::pytest.PytestAssertRewriteWarning
    # The test suite launches `psynet debug local` via `pexpect.spawn`, which
    # uses `ptyprocess` -> `pty.fork()` -> `os.forkpty()`. This DeprecationWarning
    # appears to be new in Python 3.13, and it fires when the *process* is
    # considered multithreaded.
    # We traced the warning at the fork point and confirmed that Python-level
    # threading only shows the MainThread, so the "multithreaded" state is presumably from
    # native (C-level) threads that are invisible to `threading.enumerate()`.
    # This suggests that the warning is not a problem, and we can safely silence it.
    ignore:This process .* forkpty\(\) may lead to deadlocks in the child\.:DeprecationWarning
